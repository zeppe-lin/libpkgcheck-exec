# Testing

The suite proves exact session admission, check-purpose request translation,
read-only source/package/input resources, successful and failed terminal result
mapping, stale source rejection, public-header independence, and release
metadata. Shared and static builds must be tested separately.
